name: flood
on: workflow_dispatch
jobs:
  flood:
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Flood
        run: |
          sudo apt-get install -y wrk
          wrk -t 4 -c 2500 -d 1m http://herb-cheaper-hollywood-refined.trycloudflare.com/api/update
