name: flood
on: workflow_dispatch
jobs:
  flood:
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Flood
        run: |
          sudo apt-get install wrk
          wrk -t 4 -c 100 -d 1m http://internet-her-decided-horizontal.trycloudflare.com/
